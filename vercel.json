{
  "buildCommand": "npm ci && cd packages/components && npm ci && npm run build && cd ../mobile-app && npm ci && npm run build",
  "outputDirectory": "packages/mobile-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
