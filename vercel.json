{
  "buildCommand": "cd packages/mobile-app && npm ci && npm run web && npx expo export --platform web",
  "outputDirectory": "packages/mobile-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
