{
  "buildCommand": "cd packages/mobile-app && npm ci && npm run build",
  "outputDirectory": "packages/mobile-app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
