/**
 * 🧩 STORYBOOK ENTRY POINT
 * 
 * This file is not used when running Storybook.
 * Storybook will load stories directly from the .stories files.
 */

import React from 'react';
import { Text } from 'react-native';

export default function App() {
  return <Text>Use 'npm run storybook' to view the component library</Text>;
}
